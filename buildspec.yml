version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install"
  build:
    commands:
      - echo "Preparing static website files"
artifacts:
  files:
    - '**/*'
  base-directory: 'src/main/webapp'
