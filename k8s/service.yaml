apiVersion: v1
kind: Service
metadata:
  name: sravan-web-service
  namespace: jenkins
spec:
  selector:
    app: sravan-web
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
